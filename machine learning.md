
![[Screenshot 2026-05-28 at 8.55.57 PM.png]]

mni-ml is a machine learning library with a TypeScript API and Rust backend. CUDA and WebGPU compatibility. Built to understand how ML frameworks and models work internally.

This project bridges the gap between high-level code that humans write and low-level code that hardware (CPUs and GPUs) executes.

#### Autograd Tape (Automatic Differentiation)


Machine learning is creating a mathematical function calibrated to be correct with input-output pairs. Infrastructure exists which supports this type of “training.” These ML frameworks include, but are not limited to, PyTorch, TensorFlow, JAX, Keras, Scikit-learn, and NumPy.

Gradient descent describes how we get as close as possible to the expected output value.
